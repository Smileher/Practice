//队列的顺序存储结构，入队时时间复杂度为1
//出队时时间复杂度为O(n)，每次出队所有元素都要移动
//如果不移动的话空的位置就浪费了，再进入元素队满的话就是假溢出
//故把首位相连，形成循环队列，这样只需要灵活的改变front和rear指针即可
//也就是让front或rear指针不断加1，即使超出了地址范围，也会自动从头开始
//我们可以采取去摸运算处理：
//-(rear+1)%QueueSize
//-(front+1)%QueueSize
//取模就是取余数的意思，它取到的值永远不会大于除数即可